[project]
name = "aboao-github-io"
version = "0.1.0"
description = "Static personal site served locally with uv."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false
